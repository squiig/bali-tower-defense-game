namespace Game.Audio
{
    /// <summary>
    /// The possible audio commands the system supports
    /// </summary>
    public enum AudioCommands : byte
    {
        PLAY,
        STOP_CONTEXT,
    }
}